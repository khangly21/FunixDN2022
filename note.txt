- sau khi tạo thư mục Lab03_1
- git init
- hoàn tất dự án React trong thư mục Lab03_1
- git branch
    ** Lúc này không có kết quả nào (vì chưa có nhánh nào trong local git)
- git branch Lab03_1
    ** Fatal error: invalid object named "master" (vì nhánh master chỉ được tạo ra sau lệnh commit đầu tiên)
- git add .
- git commit -m "first commit to create branch master"
- git branch 
    * master
- git branch Lab03_1
- git branch 
    Labo3_1
    *master 
- git checkout Lab03_1
   ** Switched to branch 'Lab03_1' 
- git branch
    *Lab03_1
    master
- git add .
   (lúc này không có gì staged hết vì toàn bộ nội dung thư mục Lab03_1 đã commit vào branch master)
- git commit -m "go to branch Lab03_1"
   On branch Lab03_1, nothing to commit , working tree clean

- git switch master
- git branch 
   Lab03_1
   *master 
- bây giờ đồng bộ nội dung Lab03_1 trong local master branch tới github 's remote Lab03_1 branch 
- Kết nối remote Repository:
  git remote add origin https://github.com/khangly21/FunixDN2022.git
- đồng bộ local branch master tới remote branch Lab03_1
  git push origin Lab03_1 
  (kết quả là error: failed to push some refs to that URL)
- git push origin Lab03_1 --force
- Success!
- do file note.txt đang trong trạng thái "U" (unstaged locally) nên không có trong github
- VẬY chỉ cần commit vào local branch master, rồi đồng bộ với github's remote branch Lab03_1 , chứ không cần tạo local branch Lab03_1 

- đồng bộ note.txt từ local branch master lên github
- khi dùng "đường tắt" x v Push thì thông báo "dùng upstream "master" ??" nếu nhấn yes thì nghĩa là nó tạo branch mới tên "master" trên github :( 
    ** có thông báo trên vì mặc định là local master branch thì push to remote master branch, nếu đồng ý thì trên github được tạo 1 branch mới tên "master" và nhận nội dung được push    
    ** sau này khi local master branch có thay đổi rồi git push origin Lab03_1 là vô ích vì local master branch đã kết nối remote master  
    
- khi dùng đường tắt x v "Push to" thì có lựa chọn:
    Choose a remote to publish to branch "master" to:
      Choice 1: origin   https://github.com/khangly21/FunixDN2022.git
      (chứng tỏ remote URL trên được gắn tag là 'origin')

- git push --set-upstream origin <branch name> <==??

- giả sử lỡ dùng đường tắt làm cho local branch master tracks upstream remote master branch. Điều này là  khi "git push" sẽ tạo branch mới remotely là master nhận tất cả dữ liệu từ local master branch master track upstream remote master branch
   ** Solution: 1/ current branch: master  2/ "git branch -u origin/Lab03_1"  thì kết quả là "Branch master set up to track remote branch Lab03_1 from origin!"

- thường thì đường tắt sẽ không Push được do báo lỗi refs , nên phải dùng "git push origin Lab03_1 --force "

- https://www.git-tower.com/learn/git/faq/track-remote-upstream-branch/
- https://stackoverflow.com/questions/171550/find-out-which-remote-branch-a-local-branch-is-tracking